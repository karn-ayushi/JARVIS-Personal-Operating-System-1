I am extending an EXISTING project called:

JARVIS
Journey-Aware Recall & Virtual Intelligence System

This is an existing full-stack web application.

IMPORTANT:
DO NOT rebuild the project from scratch.
DO NOT replace the existing architecture unnecessarily.
DO NOT remove or break existing working functionality.

First inspect the entire repository and understand the existing implementation.

========================================================
1. EXISTING FUNCTIONALITY TO PRESERVE
========================================================

The application already has:

- React frontend
- FastAPI/Python backend
- Gemini-powered AI Chat
- Existing authentication
- Existing persistent memory functionality
- Existing database implementation
- Existing JARVIS UI/navigation

Preserve all of these.

Before modifying anything:

1. Inspect the repository structure.
2. Inspect frontend components/pages.
3. Inspect backend routes/services.
4. Inspect database models.
5. Inspect authentication.
6. Inspect Gemini integration.
7. Inspect existing memory implementation.
8. Inspect environment variables.
9. Inspect existing dependencies.
10. Identify reusable components.

Then extend the existing application.

Do not create duplicate implementations when something already exists.

========================================================
2. JARVIS PRODUCT VISION
========================================================

JARVIS is a Personal AI Operating System.

The central idea is:

"AI remembers conversations.
JARVIS remembers the person."

JARVIS should combine:

- Persistent personal memory
- AI conversations
- Document knowledge
- Goals
- Meetings
- Tasks
- Reminders
- Personal growth
- Activity analytics
- Automated actions

The architecture should follow this principle:

GEMINI
=
Reasoning / Intelligence

JARVIS
=
Memory + Context + Personal Knowledge

IBM watsonx Orchestrate
=
Actions + Automation + Workflows

========================================================
3. TARGET ARCHITECTURE
========================================================

Use this conceptual architecture:

                    JARVIS
                       |
                  React Web App
                       |
                  REST API
                       |
                FastAPI Backend
                       |
        +--------------+--------------+
        |              |              |
     Memory          RAG           Goals
        |              |              |
        +--------------+--------------+
                       |
                Context Builder
                       |
             +---------+---------+
             |                   |
          Gemini           Orchestrate
             |                   |
        Intelligence        Actions
                                  |
                     +------------+------------+
                     |            |            |
                  Reminders     Tasks       Workflows


Database:
PostgreSQL or the existing database

Vector database:
Use the existing vector DB if available.
Otherwise use ChromaDB for the RAG prototype.

========================================================
4. FEATURE PRIORITY
========================================================

Implement features in this order:

PHASE 1 — CORE
1. Document Intelligence / RAG
2. Goals & Goal Engine

PHASE 2 — INTELLIGENCE
3. Meeting Intelligence
4. Central Context Builder

PHASE 3 — PERSONALIZATION
5. Growth Dashboard
6. Memory Timeline

PHASE 4 — KNOWLEDGE + AUTOMATION
7. Unified Knowledge Hub
8. Smart Reminders using IBM watsonx Orchestrate

PHASE 5 — ENGAGEMENT
9. Gamification
10. Analytics

PHASE 6 — FINAL POLISH
11. Dashboard integration
12. Error handling
13. Security
14. Testing
15. Responsive UI

Do not move forward while earlier critical functionality is broken.

========================================================
5. DOCUMENT INTELLIGENCE / RAG
========================================================

Priority: MUST HAVE.

Implement REAL PDF RAG.

User flow:

Upload PDF
    ↓
Extract text
    ↓
Clean text
    ↓
Chunk text
    ↓
Generate embeddings
    ↓
Store vectors
    ↓
Retrieve relevant chunks
    ↓
Context Builder
    ↓
Gemini
    ↓
Answer

Create/extend document functionality.

Document should store:

- id
- user_id
- filename
- title
- file_size
- upload_date
- processing_status

Implement:

POST document upload
GET documents
GET individual document
DELETE document
POST document query

Adapt endpoint naming to the existing API style.

Requirements:

- PDF upload
- PDF validation
- Text extraction
- Chunking
- Embeddings
- Vector storage
- Semantic retrieval
- Gemini answer generation
- Source/reference display

VERY IMPORTANT:

Retrieval must be scoped to the authenticated user.

A user must NEVER be able to retrieve another user's documents.

Example:

User uploads:

"JARVIS Architecture.pdf"

Then asks:

"What technologies are used in this project?"

JARVIS retrieves relevant chunks and answers using the document context.

Gemini should be instructed not to invent information that is not supported by retrieved context.

========================================================
6. GOALS & GOAL ENGINE
========================================================

Priority: MUST HAVE.

Create a real goal system.

Users can:

- Create goals
- Edit goals
- Delete goals
- Complete goals
- Set deadlines
- Set priorities
- Set categories
- Add milestones
- Update milestone progress

Goal fields:

id
user_id
title
description
category
priority
status
progress
deadline
created_at
updated_at
completed_at

Milestone fields:

id
goal_id
title
description
status
progress
due_date
completed_at

Create a polished Goals page.

Show:

- Active goals
- Completed goals
- Progress
- Deadlines
- Priority
- Milestones
- Goal details

Goals MUST integrate with JARVIS AI.

Example:

Goal:
"Complete JARVIS prototype"

Deadline:
September 20

User asks:

"What should I focus on today?"

JARVIS should use the active goal and deadline when generating the answer.

========================================================
7. MEETING INTELLIGENCE
========================================================

Priority: HIGH.

Allow users to:

- Paste meeting transcript
- Upload transcript/text file

Use Gemini to extract:

- Meeting summary
- Key discussion points
- Decisions
- Action items
- Tasks
- Deadlines
- Participants
- Important topics

Meeting model:

id
user_id
title
transcript
summary
decisions
action_items
participants
meeting_date
created_at

Action item structure:

task
owner
deadline
status

Create Meetings page.

Display:

Meeting
 ↓
Summary
 ↓
Decisions
 ↓
Action Items
 ↓
Tasks

IMPORTANT:

Meeting information should be able to contribute to the user's:

- Memory
- Goals
- Tasks
- Reminders

Do not fabricate information.

========================================================
8. CENTRAL CONTEXT BUILDER
========================================================

THIS IS ONE OF THE MOST IMPORTANT PARTS OF THE PROJECT.

Do not make the new features isolated CRUD pages.

Create a centralized Context Builder service.

When the user asks JARVIS a question, determine relevant context from:

1. Recent conversation
2. Persistent memories
3. Relevant documents
4. Active goals
5. Recent meetings
6. Tasks
7. Reminders
8. Recent activity

Flow:

User Query
    ↓
Context Builder
    |
    +-- Memory Retrieval
    |
    +-- Document RAG
    |
    +-- Goal Retrieval
    |
    +-- Meeting Retrieval
    |
    +-- Task Retrieval
    |
    +-- Reminder Retrieval
    ↓
Context Package
    ↓
Gemini
    ↓
Personalized Answer

Example:

User:

"What should I work on today?"

Context Builder retrieves:

- Active goals
- Upcoming deadlines
- Incomplete milestones
- Reminders
- Recent activity

Gemini produces a personalized response.

Another example:

User:

"What did we decide about the RAG architecture?"

Context Builder searches:

- Meeting records
- Memories
- Documents

Then Gemini answers using the retrieved context.

========================================================
9. GROWTH DASHBOARD
========================================================

Priority: HIGH.

Create a Growth page based ONLY on real user data.

Show:

- Active goals
- Completed goals
- Goal completion rate
- Completed tasks
- Memories created
- Documents uploaded
- Meetings processed
- XP
- Level
- Streak
- Skills/categories

Do not hardcode numbers.

Example:

LEVEL 4

720 XP

██████████████░░
78% to next level

GOALS
8 active
12 completed

ACTIVITY
34 memories
7 documents
5 meetings
42 tasks

SKILLS

AI
Python
RAG
Project Management

Only show skills that can be reasonably derived from actual user activity/data.

========================================================
10. MEMORY TIMELINE
========================================================

Priority: HIGH.

Create a chronological timeline.

Events may include:

- Memory created
- Goal created
- Goal completed
- Milestone completed
- Document uploaded
- Meeting processed
- Achievement earned
- Task completed

Example:

SEPTEMBER 7

🧠 Memory
Started implementing RAG pipeline

SEPTEMBER 6

📄 Document
Uploaded project architecture PDF

SEPTEMBER 5

🎯 Goal
Created "Complete JARVIS prototype"

SEPTEMBER 4

🏆 Achievement
Completed first milestone

Filters:

ALL
MEMORIES
GOALS
DOCUMENTS
MEETINGS
ACHIEVEMENTS

Use actual timestamps.

========================================================
11. UNIFIED KNOWLEDGE HUB
========================================================

Priority: MEDIUM.

Create a global knowledge search.

Search across:

- Memories
- Documents
- Meeting summaries
- Goals
- Notes
- Tasks

Example query:

"JARVIS architecture"

Possible results:

DOCUMENT
Relevant PDF section

MEMORY
Previous memory about architecture

MEETING
Architecture discussion

GOAL
Architecture milestone

The search MUST be user-specific.

Use semantic search where appropriate.

If the application already contains a Knowledge page, extend it instead of creating a duplicate.

========================================================
12. SMART REMINDERS
========================================================

Priority: MEDIUM/HIGH.

This feature should integrate IBM watsonx Orchestrate.

IMPORTANT:

Do NOT make Orchestrate the main backend.

FastAPI remains the JARVIS backend.

JARVIS remains responsible for:

- User data
- Memory
- Goals
- Context
- RAG
- Application logic

IBM watsonx Orchestrate should be used as the:

ACTION / AUTOMATION LAYER.

Architecture:

Goal
 ↓
JARVIS Goal Engine
 ↓
Reminder/Action condition
 ↓
IBM watsonx Orchestrate
 ↓
Automation / Workflow
 ↓
Action

Examples:

Goal deadline approaching
        ↓
Orchestrate workflow
        ↓
Reminder/follow-up

Milestone overdue
        ↓
Orchestrate workflow
        ↓
Reminder

Task due
        ↓
Orchestrate
        ↓
Reminder/action

For the prototype, IN-APP reminders are sufficient.

Do NOT implement email/SMS/push notifications unless already supported.

========================================================
13. IBM WATSONX ORCHESTRATE INTEGRATION
========================================================

Use IBM watsonx Orchestrate only where it adds meaningful automation value.

Do NOT create a fake integration.

First inspect whether the project already contains any Orchestrate configuration.

Use environment variables for credentials.

NEVER hardcode:

API keys
Tokens
Secrets
Passwords

Potential environment variables may include:

ORCHESTRATE_API_KEY
ORCHESTRATE_URL

BUT:

Only use variable names that match the actual Orchestrate integration/API being implemented.

If the exact Orchestrate API cannot be safely implemented using the available environment/configuration, create a clean integration service interface and document the required configuration instead of pretending the integration works.

Create a service conceptually like:

orchestrate_service

Responsibilities:

- Trigger workflow
- Send relevant goal/task context
- Handle response
- Handle API errors
- Log automation status

Do not move database logic into Orchestrate.

========================================================
14. ORCHESTRATE USE CASE — DEMO WORKFLOW
========================================================

Implement one strong, reliable Orchestrate workflow rather than many weak workflows.

Recommended workflow:

GOAL DEADLINE AUTOMATION

Example:

User creates:

"Finish RAG implementation"

Deadline:
September 10

JARVIS stores the goal.

As the deadline approaches:

JARVIS
 ↓
checks goal state
 ↓
Orchestrate workflow
 ↓
creates/updates reminder
 ↓
JARVIS dashboard displays reminder

The workflow should be demonstrable.

If Orchestrate integration is unavailable during development, keep the service abstraction clean and provide a local fallback for the prototype.

Do NOT fake a successful external workflow call.

========================================================
15. GAMIFICATION
========================================================

Priority: MEDIUM.

Implement lightweight real gamification.

XP events:

Create goal = +10 XP
Complete milestone = +25 XP
Complete goal = +50 XP
Upload document = +10 XP
Process meeting = +15 XP
Create meaningful memory = +5 XP
Complete task = +10 XP

Prevent duplicate XP for the same event.

Levels:

Level 1 = 0 XP
Level 2 = 100 XP
Level 3 = 250 XP
Level 4 = 450 XP

Keep level calculation centralized/configurable.

Achievements:

First Goal
First Document
First Meeting
First Completed Goal
5 Memories
10 Tasks Completed
7 Day Streak

Only award achievements when actually earned.

Do not fake streaks.

Track daily meaningful activity.

========================================================
16. ANALYTICS
========================================================

Priority: MEDIUM.

Create Analytics page.

Use actual user data.

Show:

- Goals created
- Goals completed
- Completion rate
- Tasks completed
- Memories created
- Documents uploaded
- Meetings processed
- Activity by day
- Activity by week
- XP progression
- Streak

Charts:

Activity over time
Goals completed per week
Goal completion percentage
XP progression

Do not hardcode chart values.

========================================================
17. DASHBOARD
========================================================

Upgrade the existing dashboard.

Do not destroy existing design.

Show:

Greeting

Today's Focus

Active Goals

Upcoming Reminders

Recent Memories

Recent Documents

Recent Meetings

Growth

XP

Level

Streak

Quick AI Chat

Example:

GOOD MORNING 👋

TODAY'S FOCUS

Finish document retrieval pipeline

GOALS

██████████████░░
78%

REMINDERS

Review RAG retrieval
Complete milestone 3

RECENT MEMORY

"You decided to use ChromaDB for vector storage."

GROWTH

Level 4
720 XP
🔥 6 day streak

All information must come from actual data.

========================================================
18. DATABASE
========================================================

Inspect the existing database first.

Extend it rather than replacing it.

Possible entities:

User
Conversation
Memory
Document
DocumentChunk
Goal
Milestone
Meeting
Task
Reminder
Activity
Achievement
UserAchievement
UserStats

Use proper relationships.

All user-specific records MUST be scoped to the authenticated user.

Users must never access another user's:

- Memories
- Documents
- Goals
- Meetings
- Tasks
- Reminders
- Analytics
- Achievements

========================================================
19. FRONTEND NAVIGATION
========================================================

Keep the current JARVIS navigation style.

Ensure the following are accessible:

Home
AI Companion
Memory
Knowledge
Goals
Growth
Meetings
Documents
Analytics
Settings

Use existing pages where possible.

Visual design:

- Modern SaaS
- Dark mode
- Light mode
- Purple/blue gradients
- Clean cards
- Professional typography
- Subtle animations
- Responsive layout

Do not make it overly flashy.

========================================================
20. API
========================================================

Follow the existing FastAPI conventions.

Add/extend APIs for:

DOCUMENTS

POST /api/documents/upload
GET /api/documents
GET /api/documents/{id}
DELETE /api/documents/{id}
POST /api/documents/query

GOALS

GET /api/goals
POST /api/goals
GET /api/goals/{id}
PUT /api/goals/{id}
DELETE /api/goals/{id}

MILESTONES

POST /api/goals/{id}/milestones
PUT /api/milestones/{id}
DELETE /api/milestones/{id}

MEETINGS

GET /api/meetings
POST /api/meetings
GET /api/meetings/{id}
DELETE /api/meetings/{id}

REMINDERS

GET /api/reminders
POST /api/reminders
PUT /api/reminders/{id}
DELETE /api/reminders/{id}

KNOWLEDGE

GET /api/knowledge/search

GROWTH

GET /api/growth

ANALYTICS

GET /api/analytics

GAMIFICATION

GET /api/gamification
GET /api/gamification/achievements

ORCHESTRATE

Create an internal service/integration rather than exposing credentials to the frontend.

Adapt endpoint names to the existing project if equivalent endpoints already exist.

========================================================
21. SECURITY
========================================================

Implement proper authorization.

Every request involving personal data must verify:

authenticated user
+
resource ownership

Do not trust user_id supplied by the frontend.

Derive the user from the authentication/session/JWT.

Never expose:

API keys
database credentials
Orchestrate credentials
Gemini credentials

to frontend code.

========================================================
22. ERROR HANDLING
========================================================

Handle:

Invalid PDF
Empty PDF
PDF extraction failure
Embedding failure
Vector database failure
Gemini failure
Orchestrate failure
Database failure
Invalid goal
Invalid deadline
Missing authentication
Unauthorized resource
Empty meeting transcript

Display useful frontend error messages.

Do not expose stack traces or secrets.

========================================================
23. ENVIRONMENT VARIABLES
========================================================

Inspect the existing environment configuration.

Do not hardcode secrets.

Reuse existing Gemini/database variables.

If required, document variables for:

Gemini
Database
Vector database
IBM watsonx Orchestrate

Update .env.example.

Do not put actual secrets in .env.example.

========================================================
24. TESTING
========================================================

After implementation, test the entire system.

Test:

1. Authentication
2. Existing Gemini chat
3. Existing memory
4. PDF upload
5. PDF extraction
6. Chunking
7. Embeddings
8. Vector retrieval
9. RAG answer
10. Goal creation
11. Goal editing
12. Milestone creation
13. Milestone completion
14. Meeting transcript processing
15. Meeting extraction
16. Memory timeline
17. Knowledge search
18. Reminders
19. Orchestrate workflow
20. XP
21. Achievements
22. Streak
23. Analytics
24. Growth dashboard
25. Main dashboard
26. User isolation

Fix errors rather than merely reporting them.

========================================================
25. PERFORMANCE
========================================================

Keep the prototype lightweight.

Do not introduce unnecessary dependencies.

Avoid:

- Large unnecessary frameworks
- Over-engineered microservices
- Complex agent systems
- Excessive abstraction

The goal is a stable hackathon prototype.

========================================================
26. WHAT NOT TO IMPLEMENT YET
========================================================

Do NOT implement these unless the existing project already has them:

- Multi-LLM routing
- Claude integration
- OpenAI integration
- Gmail integration
- Google Calendar integration
- GitHub integration
- LinkedIn integration
- Voice AI
- Complex multi-agent system
- Enterprise workflow ecosystem
- Mobile application

These belong to future scope.

========================================================
27. MOST IMPORTANT DEMO EXPERIENCE
========================================================

The final prototype should support this story:

STEP 1

Student opens JARVIS.

JARVIS knows the student's existing context.

STEP 2

Student creates:

Goal:
"Complete my AI project"

Deadline:
September 20

STEP 3

Student uploads:

AI_Project.pdf

JARVIS processes the document using RAG.

STEP 4

Student asks:

"What is the architecture of my project?"

JARVIS retrieves relevant document chunks and answers.

STEP 5

Student uploads a meeting transcript.

JARVIS extracts:

Summary
Decisions
Action Items
Deadlines

STEP 6

Relevant action items become tasks/goals.

STEP 7

Goal/reminder information is sent to the automation layer.

IBM watsonx Orchestrate handles the appropriate workflow.

STEP 8

JARVIS dashboard updates:

Goal progress
Tasks
Reminders
XP
Achievements
Activity

STEP 9

Memory Timeline records important events.

STEP 10

Student asks:

"What should I focus on today?"

Context Builder retrieves:

Goals
Documents
Memories
Meetings
Tasks
Reminders
Recent activity

Gemini produces a personalized answer.

This is the core JARVIS experience.

========================================================
28. PRODUCT POSITIONING
========================================================

Do not describe JARVIS as simply:

"An AI chatbot."

Position it as:

"An intelligent personal context and automation layer around AI."

Core differentiation:

Traditional AI:

User
 ↓
Question
 ↓
AI
 ↓
Answer

JARVIS:

User
 ↓
Question
 ↓
Memory + Documents + Goals + Meetings + Activity
 ↓
Context Builder
 ↓
Gemini
 ↓
Personalized Answer
 ↓
Orchestrate
 ↓
Action / Automation

Core message:

"One Memory. Infinite Intelligence."

And:

"AI remembers conversations.
JARVIS remembers the person."

========================================================
29. FINAL IMPLEMENTATION REPORT
========================================================

When finished, provide:

1. Features implemented
2. Features partially implemented
3. Files created
4. Files modified
5. Database changes
6. API endpoints
7. Frontend pages/components
8. New dependencies
9. Environment variables
10. IBM watsonx Orchestrate integration details
11. Commands to run the project
12. Testing results
13. Remaining issues

IMPORTANT:

Never claim a feature is implemented if it is only a UI mockup.

If an external IBM Orchestrate integration cannot be completed because credentials/API configuration are unavailable, clearly state that and implement a clean integration layer/fallback rather than pretending it works.

DO NOT REWRITE THE EXISTING PROJECT.

EXTEND IT.

PRESERVE EXISTING FUNCTIONALITY.

MAKE THE RESULT A FUNCTIONAL, DEMO-READY HACKATHON PROTOTYPE.